'use client';
// eslint-disable-next-line no-restricted-imports
import { useSearchParams as nextUseSearchParams } from 'next/navigation';

const useSearchParams = nextUseSearchParams;
export default useSearchParams;
